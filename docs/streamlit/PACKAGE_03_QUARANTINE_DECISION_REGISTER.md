# Package 3 — Quarantine Decision Register

The preserved quarantine is comparison evidence, not an approved package. Nothing is restored automatically.

- Registered items: **33**
- `DELETE_METADATA`: **13**
- `DO_NOT_RESTORE`: **7**
- `REFERENCE_ONLY`: **2**
- `REUSE_PARTS`: **7**
- `REWRITE`: **4**

The clean application at `a60dcbd` remains the baseline. macOS `._` files must never enter Git.
